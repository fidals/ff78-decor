extends _template

block vars

block title
	title #{title}

block prepend headLinks
	link(href='https://cdnjs.cloudflare.com/ajax/libs/fancybox/2.1.5/jquery.fancybox.min.css', rel='stylesheet')

block content
	+breadcrumbPortfolio(title)
	.content
		.row.portfolio-preview
			block text
			- var i = 1
			while i <= fotosCount
				+portfolioItem(slug, i++)

block append script
	script(src='/js/fancybox.min.js')
	script(src='https://cdnjs.cloudflare.com/ajax/libs/fancybox/2.1.5/jquery.fancybox.min.js')
